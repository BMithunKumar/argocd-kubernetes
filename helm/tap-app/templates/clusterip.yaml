apiVersion: v1
kind: Service
metadata:
  name: tap-app-clusterip-{{ .Values.namespace }}
  namespace: {{ .Values.namespace }}
spec:
  type: ClusterIP
  selector:
    project: tap-app-{{ .Values.namespace }}
  ports:
    - port: {{ .Values.service.clusterip.port }}
      targetPort: {{ .Values.service.clusterip.targetPort }}
